/*
** EPITECH PROJECT, 2018
** add_node.c
** File description:
** is able to add node into the linked list
*/

#include <stdlib.h>

void add_node(list_t *node, char **args)
{
	list_t *new = malloc(sizeof(list_t));

	return (0);
}
